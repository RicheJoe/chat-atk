# 知识产权智能客服

把现有本地聊天收成商标注册客服。岗位要看的是能落地的大模型应用：对话、流式输出、RAG、工具调用。Java、MySQL、Redis、Docker 放在这条链路讲清楚之后再补。Python 不作为前置项。

## 已有

- [x] Vue 对话界面、模型选择、流式输出、停止生成
- [x] 本机 Express：会话创建、查询、删除、发消息（SSE）
- [x] Ollama 流式与非流式调用
- [x] 超长历史裁剪和摘要
- [x] 新会话系统提示已改为「知识产权流程客服」
- [x] `SYSTEM_PROMPT` 与 `DEFAULT_SYSTEM` 已一致，README 里的默认提示也已改成知识产权流程客服

## 1. 商标知识库

只做商标注册，不铺专利、版权、诉讼。每条 Markdown 带标题、摘录日期、来源（国家知识产权局页面）。费用和期限以当次官网摘录为准。

- [x] 建 `knowledge/trademark/`，一篇一个主题，按标题切块。出处是 [《商标法》（2026年修正）](https://sbj.cnipa.gov.cn/zcwj/2026/0829/40039.html)，2026-07-06 发布，自 2027-01-01 起施行
- [x] 申请主体的材料清单：见[申请常见问题指南](https://sbj.cnipa.gov.cn/cjwt/2026/0829/20096.html)（页面发布时间 2023-03-31，文末写内容于 2022 年 1 月发布）。国内自然人需申请书、图样、个体工商户营业执照复印件、身份证明；国内法人需加盖公章的申请书、图样、带统一社会信用代码的身份证明。执照注销后如何办理，本页没有
- [x] 尼斯分类规则：[申报指南](https://sbj.cnipa.gov.cn/spfwfl/2026/0910/53830.html) 写明 45 类，并给出手套、咖啡饮料、广告片、野营住宿等例子
- [x] 五个常见类的标题、注释和类似群：摘自[区分表（尼斯分类第十二版 2025 文本）](https://sbj.cnipa.gov.cn/res/shangbiaoju/static/sbj/tem/202609/%E7%B1%BB%E4%BC%BC%E5%95%86%E5%93%81%E5%92%8C%E6%9C%8D%E5%8A%A1%E5%8C%BA%E5%88%86%E8%A1%A8--%E5%9F%BA%E4%BA%8E%E5%B0%BC%E6%96%AF%E5%88%86%E7%B1%BB%E7%AC%AC%E5%8D%81%E4%BA%8C%E7%89%88%EF%BC%882025%E6%96%87%E6%9C%AC%EF%BC%89.pdf)。全部标准项目名称尚未抄入，申报时仍要填具体项目，不能填类别标题
- [ ] 官费金额：第八十六条规定收费标准另定。现有页面没有金额，`estimate_fee` 在摘到收费页之前不能报数
- [x] 流程：九个月审查、驳回不予公告、公告之日起二个月异议、核准发证；驳回通知送达之日起十五日内可复审。2027-01-01 前不要把这些期限说成现行有效
- [x] 边界：第二十条只规定不得相同或近似，没有通过率。`常见问题.md` 标了 `index: false`，不进检索

## 2. 检索（RAG）

插在 `prepareContext` 和 `streamChat` 之间。向量先放 JSON 或 SQLite，面试时说明生产可换成 pgvector 或 Elasticsearch。

- [x] 用 `bge-m3` 建向量，写入本机 Chroma 的 `trademark` 集合（余弦距离）。Markdown 更新、模型名变化、集合为空或距离不是余弦时自动重建，并删掉资料里已经没有的段落
- [x] 用户消息取最相关的 3 段；最高分低于 `0.45` 时不塞资料。实测「异议期」能命中 `02-注册流程.md`
- [x] 命中段落只放进本次请求的系统消息。长期历史只存依据的标题、更新日期、来源网址，不存资料正文
- [x] 检索提示要求带资料标题，没有资料时回答未收录。模型仍可能不遵守，例如问个体户材料时检索偏了，却仍编了一份材料清单
- [x] 气泡下方展示「依据」；助手回复按 Markdown 排版。重新打开对话后依据还在
- [x] `常见问题.md` 标了 `index: false`，切块时会跳过。没有二级标题的文件也不会进索引

## 3. 三个工具

工具用本地表和规则返回 JSON，模型负责追问和组织中文。RAG 负责有据可查的说明，工具负责可计算、可查询的事实。实现在 `src/main/bff/tools.js`。`streamChat` 用 `bindTools` 交给当前聊天模型，一次回复最多执行一轮。

- [x] `suggest_class`：商品或服务描述 → 第九、二十五、三十、三十五、四十三类的候选。对不上不编其他类；只说「食品」时要求先问清，不直接报第三十类
- [x] `list_materials`：国内自然人、国内法人、其他组织、农村承包经营户 → 材料清单。类型认不出时要求先问清，不编材料
- [x] `estimate_fee`：记下类别数，但摘录没有金额，`recorded` 为 false、`amount` 为空。问多少钱时必须调用，禁止心算
- [x] 模型先判断要不要调工具，再把工具 JSON 组织进回复。工具参数和返回不写入会话历史
- [x] 气泡上方显示「正在查询 / 已查询」，底部「依据」仍是检索命中的资料。重新打开对话后查询记录还在

## 4. 提示词

- [x] 检索提示已要求：流程和期限只依据资料；费用和材料以工具为准；费用和期限带日期；不判断能否注册、是否近似或是否侵权
- [x] 工具约束已写在 `toolGuide`：未收录金额时回答未收录，并带上工具返回的更新日期和来源标题
- [ ] 角色仍是「你是知识产权流程客服，用中文回答。」还没写明是流程咨询、不是律师
- [ ] 输出还没固定成小标题：结论、依据、下一步

## 5. 三条演示路径

准备 8 到 12 条固定话术，现场按这三条走。

- [ ] 简单问答：「商标注册大概要多久」「一类大概多少钱」「个体户能申请吗」。回答带引用标题
- [ ] 办事引导：用户说「我想注册一个商标」。按顺序问申请人类型、商标名称、商品或服务、是否已有图样，然后调用工具给出类别、材料和费用
- [ ] 边界：「这个名字和某某大牌像不像，能不能过审」。拒绝给出授权结论
- [ ] 输入框上方放推荐问题；界面放一条固定免责声明（流程咨询，不构成法律意见）

## 6. 评测

- [ ] 写 10 条问题，每条标定期望引用的段落或期望调用的工具
- [ ] 答错时归类：检索没召回、召回了但模型不用、工具参数错、超出范围却硬答
- [ ] 每次改提示词、切块或阈值后重跑这 10 条，记录哪一类变好或变差
- [ ] 估算一次商标咨询送进模型的字数：系统提示、资料、历史、工具结果各占多少。资料过长时先缩短再送入

## 7. 文档（对应岗位里的需求说明和方案）

- [ ] 一页需求说明：用户是谁、三条演示路径、不做什么
- [ ] 一页技术方案：现有链路，以及检索和工具插在哪；说明会话可迁到 MySQL、热点检索可放 Redis、服务可放进 Docker
- [ ] 画一张与现有 Express 路由对应的 Spring Boot 模块图（Controller、检索、工具），先能讲清楚，不要求先重写

## 明确后做

- [ ] Java / Spring Boot 的薄接口，能对应现在的发消息、检索、工具调用
- [ ] MySQL 存会话，Redis 存热点检索，Docker 把模型和 API 一起启动
- [ ] 向量从 JSON / SQLite 换成 pgvector 或 Elasticsearch 的最小例子
- [ ] 够用的 Python：能读别人的切块或评测脚本、改参数。

## 先不做

- 专利、版权、诉讼咨询
- 管理后台（知识库先用带日期的 Markdown）
- 把桌面端重写成 Java
